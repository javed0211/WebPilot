# Python deps for browser-use execution path (core/browser_use_runner.py)
# Requires Python 3.11+ — use: npm run setup  (creates .venv with python3.12)
browser-use>=0.12.0
openai>=1.0.0
PyYAML>=6.0
python-dotenv>=1.0.0
