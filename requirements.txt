# Python deps for the WebPilot Browser Use adapter.
# Requires Python 3.11+ — use: npm run setup  (creates .venv with python3.12)
#
# Browser Use is vendored from upstream tag 0.13.4 so WebPilot can customize
# the agent, browser, DOM, tools, and observability internals. Keep this editable.
# Do NOT install the [video] extra — BA discovery does not use ffmpeg screencast.
# Report video is Playwright .webm (ActHistory replay / generated specs).
-e "./packages/browser-use"
openai>=1.0.0
PyYAML>=6.0
python-dotenv>=1.0.0
