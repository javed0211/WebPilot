# Packages

Third-party or independently versioned runtime source lives here.

`browser-use/` is the upstream Browser Use engine. WebPilot-specific orchestration
must stay in `integrations/browser_use/` unless a documented upstream patch is
required.
